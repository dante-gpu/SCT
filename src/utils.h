#ifndef SCT_UTILS_H
#define SCT_UTILS_H

#include <tuple>
#include <string>

std::string format_similarity_result(const std::string& file1, const std::string& file2, float similarity);

#endif // SCT_UTILS_H
